{
  "name": "authz-monorepo",
  "private": true,
  "workspaces": [
    "authz-lib",
    "examples/*"
  ],
  "scripts": {
    "build": "npm -w @sistemas-fsa/authz run build",
    "dev:express": "npm -w authz-express-app run start",
    "dev:nest": "npm -w authz-nest-app run start",
    "clean": "rimraf node_modules package-lock.json **/node_modules **/package-lock.json"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.0.0",
    "keycloak-admin-client": "^0.12.0",
    "yaml": "^2.8.1"
  }
}
